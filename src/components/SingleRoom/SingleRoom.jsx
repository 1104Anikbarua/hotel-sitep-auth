import React from 'react';

const SingleRoom = (props) => {
    const { room } = props;
    console.log(room)
    return (
        <div className='w-full max-w-lg'>

        </div>
    );
};

export default SingleRoom;